# Boucle #63 — Planches photo 7 & 8 : les 47 exercices ont une vraie photo · build 1.8.6

**Contexte :** Adrien a généré (ChatGPT, à partir du prompt fourni) les 2 dernières planches d'illustrations, avec le même personnage et le même fond bleu nuit que les 6 précédentes.

## Intégré

Deux sprites 1536×1024 (grille 3×2) ajoutés dans `assets/` :
- **`exercise-illustrations-v7.png`** (planche 7) : p0 Squat sauté · p1 Fentes sautées · p2 Montées de genoux · p3 Sauts de cheville · p4 Pont fessier une jambe · p5 Good morning kettlebell.
- **`exercise-illustrations-v8.png`** (planche 8) : p0 Nordic curl · p1 Turkish get-up kettlebell · p2 Équilibre unipodal · p3 Planche touches d'épaule (p4/p5 vides).

→ `EXERCISE_ART` mappe les 10 derniers exercices ; `.sheet-7`/`.sheet-8` ajoutées dans `strength.css` (mêmes positions `.art-p0..p5`). **Les 47 exercices ont désormais une vraie photo d'humain — plus aucun repli figure SVG** (le repli reste pour un nom inconnu, filet de sécurité).

## Vérifs

- `npm run verify` → **117 tests / 117 pass**, **SMOKE OK**.
- `icons.test.js` renforcé : regex `sheet-[1-8]`, assertions planches 7/8 (Squat sauté→7p0, Good morning→7p5, Nordic curl→8p0, Planche touches d'épaule→8p3), et **invariant : `exercises.every(...)` rend une photo** (aucun ex-figure sur un vrai exercice).
- **Rendu vérifié par capture** (galerie des 10 fiches via le vrai CSS sprite, capturée en PNG Electron) : chaque case affiche **la bonne pose, personnage entier, centré, aucune coupe**, alignement parfait — le bug historique de case décalée ne se reproduit pas.

## Détail technique

- Noms mappés au caractère près (apostrophe typographique « Planche touches d'épaule », accents « Montées », « Équilibre ») — extraits programmatiquement des données pour éviter tout mismatch silencieux.
- Assets copiés depuis `D:\Google\ChatGPT Image Jul 9 …` ; poids ~1,5 Mo chacun, cohérent avec les planches existantes.
