# Boucle #43 — Audit UX #2, passe 1 (dashboard + exercices)

**Date :** 2026-07-07
**Version :** 1.5.2 → 1.5.3

## Contexte
Adrien a coché **tous** les chantiers de l'audit UX #2. Réalisé en 2 passes.
**Passe 1 = dashboard + exercices** (A1, A2, A3, C1).

## Ce qui a été fait
- **A1 — Bloc « Aujourd'hui » fusionné** : « Ma journée » (rendez-vous/blocs) et « À faire » (To-Do) réunis dans **un seul panneau**, avec deux sous-titres (« 🗓️ Rendez-vous & blocs du jour » / « ✅ À faire (hors rendez-vous) »). Le panneau `todo-panel` séparé est supprimé ; `#todoForm`/`#todoList`/`#myDayList` déplacés dans le même bloc (mêmes IDs → aucun changement de logique).
- **A2 — Mini-agenda semaine retiré** : la section `.agenda-panel` (« Ta semaine en blocs ») du dashboard, doublon de l'onglet Agenda, est supprimée (HTML + `pageGroups` + rendu `#weekCalendar` dans `renderCommandCenter` + handler). 
- **A3 — Replis par défaut** : Mission Control + Boussole restent repliés par défaut (installation neuve) — déjà en place, conservé.
- **C1 — Recherche Exercices collante** : la barre recherche + filtre + compteur (`.exercise-controls`) devient **`position:sticky`** en haut du catalogue (fond opaque `--card`, ombre légère) → on filtre sans remonter les 7 écrans de fiches.

## Mesure avant/après (dashboard)
- Sections visibles : **9 → 7**.
- Bloc « Aujourd'hui » : To-Do **et** Ma journée dans le même panneau (vérifié), sous-titres présents, mini-agenda parti.
- Exercices : `.exercise-controls` → `position: sticky` (vérifié).

## Vérifications
- `node --test` → **81/81** ✅ ; smoke → `SMOKE OK` (myDay/todo toujours OK malgré la fusion).
- Aucune référence orpheline à `weekCalendar`/`agenda-panel`/`todo-panel`.

## Passe 2 (à suivre → 1.5.4)
- **B1** retirer les blocs Ultra-trail en double d'Athlète · **B2** sections repliables sur Athlète.
- **D1** réglages de l'agenda (Vue mois) en sections repliables.
- **E1** rappel Windows le matin d'un anniversaire.

_Note process : build fait **sans** tuer l'app d'Adrien (cf. mémoire [[irl-build-ne-pas-tuer-app]])._
