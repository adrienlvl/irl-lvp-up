# Boucle #117 (autonome) — Statistiques « à vie » · build 1.9.51

**Contexte :** 42ᵉ itération de la boucle autonome. Aire : Focus / gamification (RPG de vie).

## Livré

En tête de la **Collection de trophées** (page Focus), une rangée de cartes affiche les **totaux cumulés depuis le début** :

> 🏋️ 42 séances · ⏱️ 31 h de sport · 🏃 128 km courus · 🧠 14 h de focus · ⭐ 3 120 XP total

Une vue « à vie » motivante qui donne du sens à la régularité — parfaitement dans l'esprit RPG de l'app.

## Détail technique

- `lib/logic.js` : `lifetimeStats(state)` pur + testé — total de séances, minutes de sport, km courus (runs), blocs et minutes de focus, XP total ; défensif (arrays/valeurs manquants).
- `app.js` : `renderRoadmapFeatures` (section succès) remplit `#lifetimeStats` (minutes converties en heures au-delà de 60 min).
- `index.html` / `style.css` : conteneur `#lifetimeStats` + cartes `.life-stat`.

## Vérifs

- `npm run verify` → **157 tests / 157 pass** (+1 : `lifetimeStats` — totaux, arrondi km/XP, état vide, `null`), **SMOKE OK** (`lifetime:true`).
