# Récap boucle #29 — UX : form agenda retiré (C2) + bibliothèque dans son onglet (B2)

**Quand :** 2026-07-06
**Vague :** 6 (UX) — C2, B2
**Statut :** ✅ vérifié (56/56 tests, smoke OK, checks fonctionnels OK)

## C2 — Dashboard allégé ✅
- Retiré le **formulaire d'ajout d'agenda** du dashboard (doublon de l'onglet Calendrier). La vue « semaine en blocs » (`#weekCalendar`) reste, avec un petit renvoi vers Calendrier.
- **Piège évité** : `#exportIcs` (dans le form) était référencé par la page Calendrier. Refactoré en **`exportAgendaIcs()`**, branché directement sur `#exportIcsPage`. Plus aucune référence orpheline (`agendaForm/agendaTitle/exportIcs` = 0).

## B2 — Bibliothèque dans son propre onglet ✅
- La bibliothèque (37 exercices, ~5000 px) sort de la page Athlète vers un **onglet « Exercices »** dédié (`data-page="library"`, groupe de page `library`).
- La page Athlète est d'autant **plus courte et lisible**.
- Recherche/filtre toujours fonctionnels (test : « traction » → 6 cartes).

## Vérifications
- `node --check` OK · `npm test` 56/56 · smoke `SMOKE OK`.
- Checks fonctionnels : form retiré, export OK, lib cachée sur Athlète / visible sur Exercices, nav OK, recherche OK.

## Suite (Vague 6)
- **B1** page Athlète en sous-onglets (Séance / Progrès / Nutrition) · **B3** bloc « Mes progrès » replié.
- **D1** agenda unifié · **D2** nav regroupée · puis rebuild final.

## Git
- Commit : `feat(ux): form agenda retiré du dashboard (C2) + bibliothèque dans son onglet (B2)`.
