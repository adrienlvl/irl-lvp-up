# Boucle #59 — B-3 vérifié (rien à faire) + B-4 tests élargis

**Date :** 2026-07-08
**Version :** 1.8.2 (inchangée — tests-only)

## B-3 — États vides & libellés : vérifié, déjà bon
Survol des états vides (`empty-state`, `week-insight`, `weight-chip`) : déjà cohérents (« Aucun… / Rien… Ajoute… ci-dessus »). Les **10 nouveaux exercices** sont **tous complets** (family, kind, cue, explain, goal, avoid, sets/reps) — même richesse de fiche que les anciens ; le filtre par famille les couvre. **Rien à corriger** (pas de churn inutile).

## B-4 — Couverture de tests élargie (106 → 110)
- **Intégrité bibliothèque** (`icons.test.js`) : les **47 exercices** ont toutes leurs notes de coaching (cue/explain/goal/avoid + famille + prescription). Verrouille la qualité pour les ajouts futurs.
- **Récurrence** cas limites (`logic.test.js`) : mensuel démarré le 31 (sauté en février, présent en mars), annuel un 29 février (années bissextiles seulement), hebdo au passage d'année.
- **Sécurité SSRF** : `normalizeCalendarUrl` bloque bien l'endpoint de **métadonnées cloud** `169.254.169.254`, l'**IPv6 loopback** `[::1]`, l'hôte `.local` (point final), et normalise le schéma/hôte en minuscules.
- **Habitudes** : `habitStreak` — un **jour programmé manqué** casse la série, un jour **non programmé** ne la casse pas.

## Vérifications
- `node --test` → **110/110** ✅. Aucun changement de code runtime → **pas de build ni de bump** (un installeur serait du gaspillage).

## Suite de la boucle
- B-2 (purge CSS morte, avec vérif capture) · B-5 (option : découpe rendu `app.js`).
