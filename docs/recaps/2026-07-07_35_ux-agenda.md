# Boucle #37 — UX Agenda (passe « Agenda d'abord »)

**Date :** 2026-07-07
**Version :** 1.3.0 → 1.3.1

## Contexte
Adrien : « et après fait l'UX ! ». Choix de la 1re passe = **Agenda d'abord** (l'agenda venant d'être enrichi — import .ics + priorités — mais l'ajout/import étaient un peu enterrés dans la vue mois).

## Ce qui a été fait (entrée Agenda = vue semaine)

### 1. Ajout rapide en tête de page
Barre `#weekQuickAdd` : titre + date (défaut aujourd'hui) + heure + type + priorité + **case « Journée entière »** → ajoute un bloc sans passer par la vue mois. Rafraîchit semaine + dashboard.

### 2. Filtres
`#agendaFilters` : boutons **Sport / Focus / Vie / Révision** (afficher/masquer par type) + **🔴 Haute priorité** (ne montrer que les blocs prioritaires). Filtre la grille de la semaine en direct ; l'état des boutons reflète le filtre.

### 3. Import accessible depuis l'entrée principale
Bouton **⬇️ Importer (Google/Apple)** dans la barre d'outils de la semaine → ouvre la vue mois et **défile jusqu'au bloc d'import .ics** (livré boucle #36).

### 4. Événements « journée entière »
`allDay` désormais porté par `normalizeAgendaItem` (booléen) et exposé par `todayItems` → affichage **« Journée »** (au lieu de « — »/heure) dans « Ma journée » et la vue semaine.

## Vérifications
- `node --test` → **74/74** ✅ (nouveaux : `todayItems` expose `allDay`, `normalizeAgendaItem` normalise `allDay`).
- Smoke renderer → `SMOKE OK`, check `agendaUx:true` (weekQuickAdd + 5 filtres + importIcsWeek).
- Flux réel (Electron) : ajout rapide d'un bloc horaire **haute priorité** + d'un bloc **journée entière** → tous deux dans la grille, « Journée » et 🔴 affichés ; couper « Vie » masque le bloc Vie ; « Haute priorité » ne garde que le bloc rouge. ✅

## Suite UX possible (prochaines passes)
- Confort & finitions : « retour en haut » sur pages longues, densité réglable, hiérarchie visuelle, stats compactes, états vides.
- Nouvel audit UX complet chiffré (comme juillet) sur toute l'app.
