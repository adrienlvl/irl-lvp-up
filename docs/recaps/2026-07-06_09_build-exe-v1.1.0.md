# Récap boucle #11 — Nouveau .exe : IRL LVP UP 1.1.0 📦

**Quand :** 2026-07-06 ~04h00
**Objectif :** livrer un installeur Windows avec TOUT le travail des vagues 0/1/2/S
**Statut :** ✅ construit et testé

## Livrable
- **`D:\IRL LVP UP\build-dist\IRL LVP UP Setup 1.1.0.exe`** (109 Mo)
  - Installeur NSIS : choix du dossier d'installation, par-utilisateur (pas besoin d'admin).
  - L'app packagée (win-unpacked) a été lancée et vérifiée : démarre sans erreur.
  - Electron **43** embarqué (CVE purgées), sandbox + verrouillage navigation actifs.

## Ce que contient la 1.1.0 (vs ton ancien .exe du 22-23/06)
1. **Planning de révision BTS CG** : génération de créneaux jusqu'à l'examen (page Calendrier) + **import des vraies échéances** depuis Le Grand Livre Compta (bouton « Exporter planning » ajouté là-bas).
2. **Vue « Ma journée »** sur le dashboard : séances à démarrer, révisions à valider (+15 XP), blocs, quêtes — tout trié par heure.
3. **Notifications Windows** : résumé du matin, rappel X min avant chaque bloc, bilan du soir (réglables dans le panneau Rappels).
4. **Catégorie « Révision »** (ambre) dans les calendriers + export `.ics` conforme (durées réelles, réimport sans doublon).
5. **Robustesse** : sauvegarde anti-quota, photos hors du stockage fragile, migration douce des données existantes (rien n'est perdu), gestion d'erreurs globale.
6. **Sécurité** : sandbox renderer, aucune navigation externe possible, IPC validés, imports défensifs, CSP, instance unique, icône de tray visible.

## Note d'installation
Tes données actuelles sont dans `%APPDATA%` (userData Electron) : installer la 1.1.0 **par-dessus ou à côté** ne les efface pas (même appId `com.adrien.irllvpup` → mêmes données retrouvées si tu remplaces l'ancienne installation). La migration des photos/agenda se fait automatiquement au premier lancement.

## Incident de build résolu (documenté pour la prochaine fois)
- `electron-builder` échouait : l'archive `winCodeSign` contient des symlinks macOS → besoin de privilèges admin pour l'extraire sous Windows.
- **Contournement** : extraction manuelle du cache avec 7za (`-o...\winCodeSign-2.6.0`), les 2 erreurs de symlinks darwin sont sans conséquence sous Windows. Le build passe ensuite normalement.

## Suite (roadmap restante)
- **Vague 3** (qualité) : découpage `app.js`, rationalisation des 20 CSS, rendu ciblé, purge du résiduel npm audit (chaîne electron-builder).
- **Vague 4** (réservoir features) : graphiques enrichis, export PDF hebdo, vue semaine unifiée, thème clair…

## Git
- Commit : `build: installeur IRL LVP UP Setup 1.1.0 (electron 43) + workaround winCodeSign documenté`.
